{% extends 'base.html.twig' %}

{% block title %}Plant{% endblock %}
{% block stylesheets %}
{{ parent() }}
{{ encore_entry_link_tags('plant') }}
{% endblock %}
{% block body %}
 <a class ="btn btn-primary button"href="{{ path('plant_new') }}">Ajouter une plante</a>
<div class="content-card">
    {% for plant in plants %}
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"> {{ plant.name }}</h5>
                <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                <p>Token: {{ plant.Token }}¢</p>
                {# <a href="{{ path('plant_show') }}">show</a> #}
                {# <a href="{{ path('plant_edit', {'id': plant.id}) }}">edit</a> #}
            </div>
        </div>
        <div class="content-image">
            <img class="image-plant" src="{{ plant.image }}" alt="">
        </div>
    {% endfor %}
</div>
{% endblock %}
